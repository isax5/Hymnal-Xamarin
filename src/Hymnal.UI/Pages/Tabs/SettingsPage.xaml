<?xml version="1.0" encoding="UTF-8" ?>
<mvxViews:MvxContentPage
    x:Class="Hymnal.UI.Pages.SettingsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:iOS="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:mvxViews="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms"
    xmlns:viewModels="clr-namespace:Hymnal.Core.ViewModels;assembly=Hymnal.Core"
    Title="Settings"
    x:TypeArguments="viewModels:SettingsViewModel">

    <mvxViews:MvxContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="FontSize" Value="{OnPlatform iOS=17, Android=15}" />
                <Setter Property="VerticalOptions" Value="CenterAndExpand" />
            </Style>

            <Style TargetType="Grid">
                <Setter Property="Padding" Value="15, 12" />
            </Style>

            <OnPlatform
                x:Key="HightViewCell"
                x:TypeArguments="x:Double"
                Android="70"
                iOS="50" />
        </ResourceDictionary>
    </mvxViews:MvxContentPage.Resources>

    <TableView
        HasUnevenRows="True"
        HeightRequest="50"
        Intent="Settings">
        <TableRoot Title="hola">
            <TableSection Title="Visualización y tipografía">

                <ViewCell Height="{StaticResource HightViewCell}">
                    <Grid>
                        <Label Text="Alineación de Texto" />
                    </Grid>
                </ViewCell>

                <ViewCell Height="{OnPlatform iOS=75, Android=95}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>

                        <Label
                            Grid.Row="0"
                            Grid.Column="0"
                            Text="Tamaño letra" />

                        <Label
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="15,0"
                            Text="{Binding Source={x:Reference LetterSize}, Path=Value}" />

                        <Slider
                            x:Name="LetterSize"
                            Grid.Row="1"
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            Maximum="50"
                            MaximumTrackColor="{DynamicResource PrimaryColor}"
                            Minimum="10"
                            MinimumTrackColor="{DynamicResource AccentColor}"
                            ThumbColor="{DynamicResource AccentColor}"
                            ValueChanged="LetterSize_ValueChanged"
                            Value="18" />

                    </Grid>
                </ViewCell>
            </TableSection>

            <TableSection Title="Developer">
                <ViewCell>
                    <Grid>
                        <Label Text="Ayuda" />
                    </Grid>
                </ViewCell>

                <ViewCell>
                    <Grid>
                        <Label Text="Desarollador" />
                    </Grid>
                </ViewCell>
            </TableSection>

        </TableRoot>
    </TableView>

</mvxViews:MvxContentPage>